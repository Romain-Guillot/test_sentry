git_url(ENV['MATCH_GIT_URL'])
storage_mode("git")
git_branch(ENV['MATCH_GIT_BRANCH'] || "main")

# TODO: Update these to match the bundle identifiers you set up in Xcode for
# each flavor (dev/staging/preprod/prod). The prod identifier is typically
# unsuffixed; non-prod flavors append a suffix.
app_identifier([
  "com.example.testSentry",
  "com.example.testSentry.preprod",
  "com.example.testSentry.staging",
  "com.example.testSentry.dev"
])

# The docs are available on https://docs.fastlane.tools/actions/match
